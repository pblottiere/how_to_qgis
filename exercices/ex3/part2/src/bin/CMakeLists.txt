SET(SOURCES
  main.cpp)

SET(MYLIB_HEADERS
  ${MYLIB_DIR}/view.h)

INCLUDE(${QT_USE_FILE})
ADD_DEFINITIONS(${QT_DEFINITIONS})

INCLUDE_DIRECTORIES(${MYLIB_DIR})
LINK_DIRECTORIES(${MYLIB_DIR})

QT4_WRAP_CPP(MYLIB_MOC ${MYLIB_HEADERS})

ADD_EXECUTABLE(memo ${SOURCES} ${MYLIB_MOC})
TARGET_LINK_LIBRARIES(memo ${QT_LIBRARIES})
TARGET_LINK_LIBRARIES(memo mylib)
